package com.rockthejvm.numbers
import scala.util.Random

object ApproxPi  extends  App{
  // use monte carlo method: take random point inside unit square and check to see if it lies within the circle
  // ratio of points inside circle to all generated points is p.
  // p*4 = pi
  def approximatePi(nPoints: Int):  Double = {

  }

}
